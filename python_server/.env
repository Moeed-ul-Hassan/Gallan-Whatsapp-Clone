# Environment variables for Python server
SESSION_SECRET=whatsapp-clone-secret
PORT=5001
# MongoDB URI (uncomment to use MongoDB)
# MONGODB_URI=your_mongodb_uri
# OpenAI API Key (already provided in environment secrets)
# OPENAI_API_KEY is accessed from environment secrets